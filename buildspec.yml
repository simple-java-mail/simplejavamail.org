version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: 20
        commands:
            - npm ci --no-audit --no-fund
    build:
        commands:
            - npx gulp build
artifacts:
    files:
        - '**/*'
    base-directory: dist
cache:
    paths:
        - /root/.npm/**/*